# 📍 Vous êtes ici : main — La vue d'ensemble

> Tout est là : le produit complet, la méthode, les traces.
> La carte de navigation est dans `README.md`, le déroulé pour
> l'animateur dans `GUIDE-ANIMATEUR.md`.

Pour revivre le chemin depuis le début :

```bash
git checkout step-0-setup
```

Et laissez-vous guider par le `README-ETAPE.md` de chaque branche.
